package org.shadrin.hashchecker.plugins

class UrlTemplates {
    companion object {
        const val CHECKSUMS = "/checksums"
        const val ADD_CHECKSUM = "/checksums/add"
        const val CHECKSUM_REQUEST_CHUNK_SIZE = "/checksums/chunkSize"
        const val PING = "/ping"
    }
}
